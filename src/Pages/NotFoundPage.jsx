import React from 'react';
import Layout from '../Layout/Layout';

const NotFoundPage = () => {
    return (
        <Layout>
            <h1 className='mx-auto text-center p-52 text-lg'>I usderstand. You are not so Happy for this.</h1>
        </Layout>
    );
};

export default NotFoundPage;